import * as React from 'react';
import FormWrapper from './FormWrapper';

const FormPage = () => {
    return (
        <div>
            <FormWrapper />
        </div>
    );
};

export default FormPage;